---
const { text, href } = Astro.props;
---

<a
  href={href}
  target='_blank'
  class='inline-flex items-center justify-center px-10 text-3xl py-6 font-semibold text-white
           bg-gradient-to-r from-purple-500 to-indigo-500
           rounded-md shadow-md hover:from-purple-600 hover:to-indigo-600
           focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500
           hover:scale-110 transition-all duration-300 animate-jump'
>
  {text}
</a>

<style>
  @keyframes jump {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .animate-jump {
    animation: jump 1s infinite;
  }

  a {
    font-family: "Poppins", sans-serif;
    font-weight: 700;
  }
</style>
