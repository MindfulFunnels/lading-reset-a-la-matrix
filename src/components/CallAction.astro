---
const { text, href } = Astro.props;
---

<a
  href='#membership-section'
  class='inline-flex justify-center items-center px-10 py-6 text-3xl font-semibold text-white bg-gradient-to-r from-orange-400 to-orange-600 rounded-md shadow-md transition-all duration-300 hover:from-orange-500 hover:to-orange-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-500 hover:scale-110 animate-jump'
>
  {text}
</a>

<style>
  @keyframes jump {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .animate-jump {
    animation: jump 1s infinite;
  }

  a {
    font-family: "Poppins", sans-serif;
    font-weight: 700;
  }
</style>
